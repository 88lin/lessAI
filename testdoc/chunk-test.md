# LessAI 分块测试（Markdown）

> 用途：用来查看 chunk 高亮与行内保护区高亮（如 `$...$`、`` `code` ``、链接），并核验标题是否被保护。

## 第一段：段落 + 整句

这段内容包含多句完整表述。数字 3.14 不应被误判为句号的断句点。末句以问号结束用于测试：整句切分是否会在“？”处生效？

这段内容还包含一个行内公式 $E=mc^2$，以及一段行内代码 `cargo fmt --check`；它们都需要标记为保护区，但不应打断 chunk 高亮。

## 第二段：列表（行骨架）

- 条目 A：链接 [OpenAI](https://openai.com)（需要保持链接结构）。
- 条目 B：图片语法 ![alt](https://example.com/a.png)（不必确保可访问）。
- 条目 C：强调 **加粗** 以及 *斜体*（不要破坏标记）。

## 第三段：代码块（多行保护区）

```bash
# 这段应该整体保护（不改写）
cargo fmt --check
cargo test
```

结尾一句：代码块之后的正文仍需照常分块。
